// src/pages/Admin.jsx (FINAL)

import AdminPanel from '../components/AdminPanel/AdminPanel';

export default function Admin() {
    // 1. Verificações de Autenticação/Permissão (Se necessário)
    // Exemplo: Se o usuário logado não for 'ADMIN', redireciona para a Home.

    return <AdminPanel />;
}